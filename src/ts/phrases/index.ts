import { BasePhrase } from './base';
import { ShitShow } from './shitshow';
import { ClusterFuck } from './clusterfuck';
import { HotGarbage } from './hotgarbage';
import { HotMess } from './hotmess';

export const phrases: BasePhrase[] = [
    new ShitShow,
    new ClusterFuck,
    new HotGarbage,
    new HotMess,
];
